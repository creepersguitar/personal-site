# Database Configuration
DB_URI=mongodb://username:password@host:port/database_name

# Server Configuration
PORT=5000

# Secret Keys
SECRET_KEY=your_super_secret_key
API_KEY=your_api_key_for_third_party_services

# Application Configuration
NODE_ENV=development          # Set to 'production' in the production environment

# Optional: JWT Token Secret for Auth (if using JWT for user authentication)
JWT_SECRET=another_secret_key

# Optional: Email Configuration (if needed for contact forms, etc.)
EMAIL_SERVICE=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=your_email_user
EMAIL_PASS=your_email_password

SECRET_KEY='your_actual_secret_key'
DEBUG=True
ALLOWED_HOSTS='localhost,127.0.0.1'
DB_NAME='your_database_name'
DB_USER='your_database_user'
DB_PASSWORD='your_database_password'
DB_HOST='mongo'  # Docker service name for MongoDB
DB_PORT='27017'